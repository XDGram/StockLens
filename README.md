# StockLens

Pre-trade verification, control analysis, price intelligence, and exit previews
for tokenized stocks on Solana.

## Development

```bash
npm install
npm run dev
```

Run the TypeScript checks and CLI smoke script:

```bash
npm run check
npm run script:smoke
```

The frontend intentionally remains unimplemented while the verification and
market-data foundation is built.
